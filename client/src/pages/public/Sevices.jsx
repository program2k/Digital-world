import React, { memo } from 'react';
import NotFound from './NotFound';

const Sevices = () => {
    return (
        <div>
            <NotFound/>
        </div>
    );
};

export default memo(Sevices);